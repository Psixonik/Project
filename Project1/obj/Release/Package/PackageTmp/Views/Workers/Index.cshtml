@model IEnumerable<Project1.Models.Worker>

<!--<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.colWorkers)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.colWorkers)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })
        </td>
    </tr>
}

</table>-->
<link rel="stylesheet" href="~/Content/css/Workers.css">

<form method="post" action="/Workers/Сhange">
    <div>
        <div>
            <h2>
                В цехе работают @ViewBag.worker человек<br>
                с общей зарплатой  @ViewBag.zp
            </h2>
        </div>
        <div id="button">
            <div id="buttonAdd">
                <label>Нанять рабочего<input style="margin-left:10px;" type="submit" name="change" value="add" id="addWorkers"></label>
                
            </div>
            <div id="buttonMin">
                <label>Уволить<input  style="margin-left:10px;" type="submit" name="change"
                                     value="min" id="minWorkers" @((@ViewBag.worker <= 0) ? "disabled" : "")>
                </label>
                
            </div>
        </div>
    </div>
</form>
